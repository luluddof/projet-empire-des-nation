# Dev : laisser vide — le proxy Vite envoie /api vers Flask (cookie de session sur le port 5173).
# Build de production : définir l’URL publique du backend, ex. VITE_API_BASE=https://api.mondomaine.com
# VITE_API_BASE=
